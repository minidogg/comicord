{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "unpatchBuildInject": "npm run unpatch && npm run build && npm run inject",
    "build": "pnpm exec tsc build.ts",
    "inject": "pnpm exec tsc inject.ts",
    "unpatch": "pnpm exec tsc unpatch.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.5.2"
  }
}